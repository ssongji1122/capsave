{
  "name": "capsave-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "dev:web": "turbo dev --filter=@capsave/web",
    "dev:mobile": "cd apps/mobile && npx expo start",
    "build:shared": "turbo build --filter=@capsave/shared"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
